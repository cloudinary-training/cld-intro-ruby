<div class="item">

<div class="caption">
 <%= description %>
</div>

<% options_with_format = options.merge(:format => format) %>
  <%= cl_image_tag(public_id, options_with_format) %>
  <div class="link">
    <% link_url = cloudinary_url(public_id, options_with_format) %>
    <%= link_to(link_url, link_url, :target => '_blank') %>
  </div>

  

</div>